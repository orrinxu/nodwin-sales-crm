// Stub for Next.js `server-only` guard in the Vitest environment.
// The package throws at runtime if imported in a client bundle; in tests we
// simply do nothing so server modules can be exercised without Next.js.
export {}
